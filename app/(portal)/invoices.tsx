import { ComingSoon } from '@/src/features/portal/PortalScreen';

/** Portal invoices tab — Plan 8 Task 5 replaces this body with the list/detail. */
export default function PortalInvoices() {
  return (
    <ComingSoon
      title="Invoices"
      note="Your invoices and payment history will live here."
    />
  );
}
