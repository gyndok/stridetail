import { ComingSoon } from '@/src/features/portal/PortalScreen';

/** Portal requests tab — Plan 8 Task 7 replaces this body with the request form. */
export default function PortalRequests() {
  return (
    <ComingSoon
      title="Visit requests"
      note="Ask for a walk or visit here — your provider approves it in a tap."
    />
  );
}
