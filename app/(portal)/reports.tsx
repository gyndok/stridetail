import { ComingSoon } from '@/src/features/portal/PortalScreen';

/** Portal reports tab — Plan 8 Task 5 replaces this body with the archive. */
export default function PortalReports() {
  return (
    <ComingSoon
      title="Report cards"
      note="Every visit report card will live here — photos, route, and all."
    />
  );
}
