// Thin wrapper: an owner's own Earnings without leaving the owner shell
// (tab-shell unification, 2026-09-06). Hidden from the tab bar in _layout.
export { default } from '@/src/features/billing/EarningsScreen';
