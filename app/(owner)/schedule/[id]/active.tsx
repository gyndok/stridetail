// Thin wrapper: dual-role owners walk their own visits WITHOUT leaving the
// owner shell (tab-shell unification, 2026-09-06 — Alexandra: the flip to the
// walker view was confusing). Same shared screen as (walker)/visit/[id]/active.
export { default } from '@/src/features/visit/ActiveVisitScreen';
