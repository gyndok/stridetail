// Same shared screen as the owner group's manual route; hidden from the
// walker tab bar (href: null in the layout) and reached from Settings.
export { ManualScreen as default } from '@/src/features/manual/ManualScreen';
